build/
*.tmp
.godot/
.import/
export_credentials.cfg
*.keystore
*.jks

# Godot extracts a glTF's embedded textures next to the .glb on import
# (gltf/embedded_image_handling=1). It is a build product, regenerated by
# `--import`, and a second copy of the same 830 KB atlas would otherwise ship
# twice in every export.
models/hero_hero_atlas.png
models/hero_hero_atlas.png.import
