build/
*.tmp
.godot/
.import/
export_credentials.cfg
*.keystore
*.jks

# Godot writes a .uid next to every script; they are how the editor tracks a
# script across a rename and are meant to be committed. Kept, deliberately.
